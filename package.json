{
  "name": "technova-ecommerce-lab",
  "version": "1.1.0",
  "description": "Lab de E-commerce para Testes QA - Padrão Ouro de Automação",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && cd backend && npm install && cp backend/.env.example backend/.env && cp backend/.env.example .env && rm -rf cypress/e2e/1-getting-started cypress/e2e/2-advanced-examples && echo '✅ Ambiente Blindado e Limpo!'",
    "backend": "cd backend && node index.js",
    "frontend": "npx serve frontend -p 3000",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "test": "npx cypress run",
    "test:open": "npx cypress open",
    "dev": "npm run docker:up && (npm run backend & npm run frontend)"
  },
  "keywords": [
    "qa",
    "testing",
    "cypress",
    "node",
    "postgres",
    "automation"
  ],
  "author": "André Gonçalves",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.0.0",
    "serve": "^14.2.0"
  }
}